{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Sodium",
      "type": "node-terminal",
      "request": "launch",
      "command": "qemu-system-x86_64 -debugcon stdio -cdrom ${workspaceFolder}/dist/Sodium.iso",
      "preLaunchTask": "build"
    }
  ]
}
